# Hello World (Python)

A minimal Python CLI used to verify the Dev Container setup.

## Prerequisites

Open this directory in VS Code and choose **Reopen in Container** so that Python and pytest are available.

## Run the script

```shell
python3 hello.py
```

This prints a greeting along with the Python version and environment details.

## Run the tests

```shell
pytest
```
