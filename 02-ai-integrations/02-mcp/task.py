#!/usr/bin/env python3
"""Starter file for the Context7 MCP demo.

The task: use Copilot Chat (with the Context7 MCP server enabled) to fetch
up-to-date documentation for Python's `argparse` module and implement a small
CLI below that greets a user by name, e.g.:

    python task.py --name Ada

Try a prompt like:
    "Implement main() using argparse with a --name option that defaults to
    'World' and prints a greeting. use context7"
"""


def main() -> None:
    raise NotImplementedError("Ask Copilot (with Context7) to implement this using argparse.")


if __name__ == "__main__":
    main()
